pub struct Mentee {
    // pub id: u32,
    pub name: String,
    pub calls: u32,
}

// impl Mentee {
//     pub fn new(id: u32, name: String, calls: u32) -> Self {
//         Mentee { id, name, calls }
//     }
// }
